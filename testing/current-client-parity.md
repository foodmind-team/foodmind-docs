# Current client parity contract

The current canonical Backend OpenAPI contract contains 86 operations. This includes the public curated-catalogue image endpoint, while private record media continues to use authorised signed URLs.

Web and Android keep versioned contract snapshots and fail their API checks when the Backend source, snapshots, generated metadata, or declared operation coverage drift. Update all three repositories together for an approved public-contract change.
